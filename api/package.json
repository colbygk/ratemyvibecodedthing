{
  "name": "rmvct-api",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker API for ratemyvibecodedthing.ai (Upstash Redis + R2).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.80.0"
  }
}
