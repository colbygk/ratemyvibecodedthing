node_modules/
dist/
.env
.env.local
.dev.vars
.wrangler/
.DS_Store
*.log

# Production data snapshots from api/scripts/backup-redis.mjs — contain pwhashes.
# NEVER commit. Keep them out of the repo.
redis-backup*.json
backups/
